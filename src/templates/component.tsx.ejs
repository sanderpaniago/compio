type <%= props.name %>Props = {
  example: string
}

export default function <%= props.name %>({example}: <%= props.name %>Props) {
  return (
    <>
      {example}
    </>
  )
}
<% if (props.hasSchema) { %>
<%= props.name %>.schema = {
  title: "<%= props.name %>",
  type: "object",
  properties: {
    example: {
      title: "example title",
      type: "string"
    }
  }
}
<% } %>